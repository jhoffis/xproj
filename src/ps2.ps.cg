struct vOut {
	float4 col  : COLOR;
	float2 tex0 : TEXCOORD0;
};

float4 main(vOut I,
	uniform sampler2D colorMap) : COLOR
{
    float2 coor = float2(I.tex0.xy);
    coor.x *= 0;
	float4 color = tex2D(colorMap, coor);
	return color;
//	return float4(I.col.xy, 0, 1);
}
