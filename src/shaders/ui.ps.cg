struct vOut {
	float4 color : COLOR;
    // float2 tex0 : TEXCOORD0;
};

float4 main(vOut I) : COLOR { // , uniform sampler2D colorMap) : COLOR {
    return float4(1,0,0,1); // tex2D(colorMap, I.tex0);
}
