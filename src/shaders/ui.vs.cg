struct vIn {
    float2 tex      : TEXCOORD;
    float4 position : POSITION;
};

struct vOut {
    float3 pos  : POSITION;
    float2 tex0 : TEXCOORD0;
};

vOut main(vIn I) {
    vOut o;
    o.pos = float3(I.position.x, I.position.y, 0.0f);
    o.tex0 = I.tex;
    return o;
}
